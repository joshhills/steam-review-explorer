const ReviewVolumeDistributionBarChart = ({ reviews }) => {
    const mungeReviewData = (reviews) => {
    }

    const data = mungeReviewData(reviews)

    return (
        <p>Test</p>
    )
}

export default ReviewVolumeDistributionBarChart